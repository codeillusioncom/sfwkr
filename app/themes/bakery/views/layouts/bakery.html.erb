<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <%= display_meta_tags site: Settings::Setting.get_system_setting('title') %>
    <%= stylesheet_link_tag "#{Settings::Setting.get_system_setting('theme')}/all", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "#{Settings::Setting.get_system_setting('theme')}/all", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.1/css/lightbox.css">
</head>

<body>
    <%= render 'shared/modals' %>
    <%= render 'fragments/notifications' %>
    <% @page.fragments.order(:order).each do |fragment| %>
      <%= render "fragments/#{fragment.fragment_name}", fragment: fragment %>
    <% end %>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>

    <sscript src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.1/js/lightbox-plus-jquery.js"></sscript>
    <sscript src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.1/js/lightbox.js"></sscript>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
</body>

</html>